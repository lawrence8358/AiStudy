# 程式碼審核開發

## 📊 功能正確性檢查
> 情境：調整 Task 預設工具和要求檢查功能正確性

``` markdown 
AI-PR-AutoReview 的程式碼審核有多個模式，目前我多加了一個精準行號標註的功能(請參考 Git 簽出的檔案)，因此支援的模式多樣且不同的排列組合會造成不同的審核結果，但我需要再次確定程式和功能的正確性，我需要你協助調查程式碼，並進行底下幾個步驟。

1. inputSystemInstructionSource 請增加 Built-in(排序在第一個，並且為預設值)，當使用者選擇此欄位時，不使用 inputSystemPromptFile 和 inputSystemInstruction ，而是使用內建的 SystemPrompt，同時也要控制 inputSystemPromptFile & inputSystemInstruction 兩個在 task.json 的顯示狀態。
2. 調整 task.json 的審核狀控制預設值如下，
	+ GitHubCopilotTimeout=120000
	+ GitHubCopilotTimeout=300000
	+ MaxOutputTokens，調整成非必填，且無預設值，若無設定，則 CLI 呼叫時則不設定此參數
	+ Temperature=0.2
	+ EnableThrottleMode=true
	+ ShowReviewContent=true
	+ EnableIncrementalDiff=true
	+ EnableInlineComments=true
	+ GroupInlineCommentsByFile=false
	+ InlineStrictMode=false
3. 確認 instructions.constants.ts ,當參數 InlineStrictMode = true 時，才需要出現建議，但若該值為 false 時，之前會發生，屬於建議類型的直接變成警告，因此適當的調整 Prompt 讓他可以正確的分類。
4. 請深度的檢查目前的程式是否有漏洞(例如與功能不符)，不能只是看務必執行 npm run debug (為了比對所有的功能，請調整 .env 內的參數，已確保所有參數和場景都有執行並驗證過，但**禁止修改模型參數**) 並記錄執行後審查結果(AI Code Review 後的回應)，並補充到 PARAMETER-COMBINATIONS.md 和 PARAMETER-COMBINATIONS.zh-TW.md  場景範例中，請務必要每個模式都執行到。
5. 功能已修復，請一併檢查 PARAMETER-COMBINATIONS.md 和 PARAMETER-COMBINATIONS.zh-TW.md & README.md & README.zh-TW.md & README-Dev.md 文件是否與現行功能不符，若不符請修正。
6. .env 的註解也幫我調整正確(中英文顯示和正確歸類分類)

還有請記住，除了調派外，你不允許直接執行(也不能分析)，任何動作都請派遣子代理進行(並記錄執行 Task 到 一個 md 檔案)，請反覆執行上述的檢查項目 3 次，比對該 Task 是否都已完成，已確保滿足需求
```